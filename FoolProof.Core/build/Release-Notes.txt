- New validation attributes (**AndPredicateAttribute**, **OrPredicateAttribute** and **NotPredicateAttribute**) to support complex validation rules through boolean predicates.
- Client side support for the *predicates* validators (jQuery validation only).
- Support for model-wise validation rules (using *predicate* validators and `TargetPropertyName`).