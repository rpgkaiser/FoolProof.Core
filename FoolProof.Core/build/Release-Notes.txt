- Use a default Enum value instead of a nullable Enum, for the `DataType` property.
- Improve data type handling for the client-side validation.
- Fix client-side validation logic for `In` and `NotIn` validators.
- Use constructor-based default messages, instead of a property.
- Improve Unit and E2E tests.